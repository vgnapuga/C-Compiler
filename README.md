# ***Отчёт*** *(компилятор подмн-ва языка C)*

## **Функционал** {
### ***Аттестация 1:***
**типы**
- `int`, `char`, `bool`, `void`
- одномерные массивы

**операции**
- `+`, `-`, `*`, `/`, `%`
- `==`, `!=`, `<`, `>`, `<=`, `>=`
- `=`
- `!`

**операторы**
- `if / else`
- `for`, `while`, `do while`
- `return`

**функции**
- объявление и вызов
- передача парамтров
- возврат значений
#### \----------------------------------------------------------------------------------
### ***Аттестация 2:***
#### \----------------------------------------------------------------------------------
### ***Аттестация 3:***
## }

## **Запуск** {
### ***Через бинарь:***
```bash
./compilator test.c
```
#### \----------------------------------------------------------------------------------
### ***Исходники через Cargo (если есть пакет):***
```bash
cargo run -- test.c
```
#### \----------------------------------------------------------------------------------
### ***Исходники через Nix (если есть пакет):***
```bash
nix run .# -- test.c
```
## }
